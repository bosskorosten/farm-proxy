# Используем базовый образ
FROM node:20

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем весь проект в рабочую директорию
COPY . .

# Устанавливаем зависимости, если есть
# RUN npm install

# Указываем команду для запуска приложения (в данном случае ничего не нужно)
 CMD ["npm", "start"] # если у вас есть такая команда

# Запускаем docker-compose (не рекомендуется)
# CMD ["docker-compose", "up", "-d"] # это не будет работать как ожидается
